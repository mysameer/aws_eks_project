apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-ui-chart.fullname" . }}
  labels:
    {{- include "devops-ui-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-ui-chart.selectorLabels" . | nindent 4 }}
